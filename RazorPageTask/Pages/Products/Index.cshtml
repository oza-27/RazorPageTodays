@page
@model RazorPageTask.Pages.Products.IndexModel

@{
    ViewData["Title"] = "Index";
}
<div class="p-3">
    <div class="row">
        <div class="col-6">
            <h2 class="text-primary"> Product List </h2>
        </div>
        <div class="col-3">
           Search: <input type="text" id="searchBox" />
        </div>
        <div class="col-3 text-end">
            <a class="btn btn-primary" asp-page="Create"> Create a new Product </a>
        </div>
    </div>
</div>
<table id="tblData" class="table table-bordered table-striped text-center" style="background-image:linear-gradient(lightgrey,pink)">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Product[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product[0].IsActive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product[0].Category)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Product) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @(item.IsActive != false?"Active":"InActive")
            </td>
            <td>
                    <a asp-route-id="@item.ProductId" asp-page="/Category/Index">@Html.DisplayFor(modelItem => item.Category.Name)</a>
            </td>
            <td>
                <a class="btn btn-primary" asp-page="./Edit" asp-route-id="@item.ProductId">Edit</a> |
                <a class="btn btn-danger" asp-page="./Delete" asp-route-id="@item.ProductId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
@section Scripts{
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/js/product.js"></script>
}